/* const guidelist = document.querySelector('.guides');
 * 
 * const setupGuides = (data) => {
 * 
 * if (data.length) {
 *  let html = '';
 *  data.forEach(doc => {
 *      const guide = doc.data();
 *      const li = `
     *      <li>
     *          <div class="">${guide.title}</div>
     *      </li>
 *      `;
 *      html += li;
 *  });
 *  
 *  guideList.innerHTML = html;
 * } else {
 *  //Do what would happen if logged out
 * }
 * }
 */